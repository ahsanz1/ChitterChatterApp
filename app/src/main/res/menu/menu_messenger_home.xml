<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.ahsanz.chitterchatterapp.MessengerHome">

    <item
        android:id="@+id/action_settings"
        android:orderInCategory="100"
        android:title="@string/action_settings"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_signout"
        android:orderInCategory="100"
        android:title="Sign Out"
        app:showAsAction="never" />

    <item
        android:visible="true"
        android:id="@+id/action_message_requests"
        android:icon="@drawable/multiple_plus"
        android:title="Message Requests"
        app:showAsAction="always">
    </item>

    <item android:id="@+id/search"
        android:title="search_title"
        android:icon="@drawable/baseline_search_white_24dp"
        app:showAsAction="always"/>

</menu>
